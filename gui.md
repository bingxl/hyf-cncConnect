# gui 规划
使用到的库： sqlite, imgui c++
先注意，所有ui中使用的文字都使用中文，尽量不要使用英文。
因为机床大概有20多台，所有设计到从机床获取数据的操作都使用多线程进行。
gui 部分才用ui与业务逻辑分离开发

## 机床ip模块
机床ip，名字，端口信息存放到sqlite 中
1. 能列出所有机床的ip，名字
2. 能编辑/删除单个机床的信息
3. 添加机床信息

## 展示机床信息
点进该模块后先展示所有机床的名字与历史加工数量
点击某个机床后展示该机床的所有能获取到的信息

## 历史数据管理
该模块涉及到的时间展示时只展示到分钟
1. 获取历史数量， 点击后展示此时各机床的历史加工数量。
2. 保存数据， 将当前获取到的历史加工数量与机床名与时间 保存到sqlite数据库中
3. 计算， 从数据库中选择最近4批次的时间(一个批次是2中保存数据时所有机床的数据)供用户选择，默认选择最新批次的时间，计算第一个功能中获取到的各机床当前历史数据与选择批次中的历史数据的差值， 当当前机床的数据异常或者选择批次中对应机床的数据异常时显示异常
